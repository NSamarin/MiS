//Backbone.Router implemented here

define(["backbone"],
    function (Backbone) {

        var Router = Backbone.Router.extend({
            initialize: function () {

            }
        });
        return Router;
    });