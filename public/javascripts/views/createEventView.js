define(["backbone"],
    function (Backbone) {
        var createEventView = Backbone.View.extend({
            //el: $("#usertable"), // the element tag of this view
            //className: "dashboard",

            // some init
            initialize: function () {
            },

            // some render
            render: function(){

            }

            //TODO: add some functions which select tags and parses info user enters
            //eg. $(.date).val() ...





        });
        return createEventView;
    });
